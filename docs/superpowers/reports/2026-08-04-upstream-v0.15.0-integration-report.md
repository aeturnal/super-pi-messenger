# Upstream v0.15.0 Integration Report

## Source

- Upstream: `https://github.com/nicobailon/pi-messenger.git`
- Tag: `v0.15.0` at `2f5e7dc9c77fd7a3fba4728931e8564ce48d9bab`
- Shared base: `f836c400103bb3c99ff350aa9c3d0e80e58f046b`
- Fork base: `3315090`
- Integration branch: `integrate/upstream-v0.15.0`
- Upstream push URL: `DISABLED`

## Baseline Verification

Date: `2026-08-04T01:04:53-05:00`

Commands and observed results:

- `test "$(git branch --show-current)" = "integrate/upstream-v0.15.0"` — exit `0`.
- `test "$(git rev-parse 2f5e7dc)" = "2f5e7dc9c77fd7a3fba4728931e8564ce48d9bab"` — exit `0`.
- `test "$(git merge-base 3315090 upstream/main)" = "f836c400103bb3c99ff350aa9c3d0e80e58f046b"` — exit `0`.
- `test "$(git config --get remote.upstream.pushurl)" = "DISABLED"` — exit `0`.
- `git status --short --branch` — `## integrate/upstream-v0.15.0`.
- `npm ci` — exit `0`; 191 packages added and 192 packages audited. npm reported 3 vulnerabilities (1 moderate, 2 high) and install-script approval warnings for `@google/genai@1.52.0` and `protobufjs@7.6.5`.
- `npm exec tsc -- --noEmit` — exit `0`; no output.
- `npm test` — exit `0`; Vitest `45` test files passed and `607` tests passed.
- `git diff --check` — exit `0`.
- `git status --short` — no output before this report was created.

`package.json`, `package-lock.json`, and `docs/upstream-maintenance.md` were unchanged by installation and baseline checks.

## Commit Ledger

| Upstream commit | Subject | Disposition | Fork adaptations | Tests |
|---|---|---|---|---|
| `2c3b748` | Optional Crew Team layer | Adapted | Cherry-picked intact as `ad4fb91` with upstream authorship and `-x` trailer; one fork-only type import adaptation in follow-up `32d5e2d`: `@mariozechner/pi-coding-agent` → installed `@earendil-works/pi-coding-agent` in `crew/handlers/team.ts`; boundary-hardening adaptation `c3eb4fe`: controller-only approval/rejection, Crew child denial and command removal, post-role Crew-only restrictions, and bounded Team memory handling | Initial Team + worker coordination: 7 files / 61 tests; initial Superpowers boundary: 5 files / 58 tests; initial full suite: 51 files / 651 tests; boundary hardening: 13 relevant Team/Superpowers files / 134 tests; `tsc --noEmit`: pass |
| `8d24757` | Team workflows for current Pi | Adapted | Cherry-picked as `f58620e` with upstream authorship and `-x` trailer; composed current-Pi Team workflows with fork-owned Superpowers launch/policy/status, controller-only approvals, post-role Crew restrictions, bounded Team memory, fork package identity/dependency floors, and combined Team/Superpowers status. Follow-up `356aa55` hardens Crew status by separating executable, pending-approval, and rejected tasks, with distinct work, approval, and revision guidance; it also corrects changelog packaging text to match the manifest. | Team focus: 12 files / 160 tests; status hardening RED: 3 expected failures / 11 passes; GREEN: 14/14; Team/status: 163/163; Superpowers: 62/62; `tsc --noEmit` and `git diff --check`: pass; full suite: 51 files / 663 tests |
| `5cb1f53` | Backlog compatibility fixes | Retained | Cherry-picked intact as `67c994f` with upstream authorship and `-x` trailer; no conflicts or fork adaptations required. The imported worker cap composes with Team-aware readiness filtering, and the TypeBox fallback leaves fork Superpowers lifecycle wiring in `index.ts` intact. | Focused compatibility/Team/Superpowers: 4 files / 37 tests; `tsc --noEmit` and `git diff --check`: pass |
| `817e1ae` | Legacy installer package-conflict guard | Adapted | Cherry-picked intact as `5dc727a` with upstream authorship and `-x` trailer; fork adaptation `98e7ec0` retains the legacy `pi-messenger` CLI/directory while recognizing `npm:pi-messenger`, `npm:super-pi-messenger`, `git:github.com/aeturnal/super-pi-messenger`, and `git:github.com/aeturnal/super-pi-messenger@<ref>` settings sources, plus either native npm directory. | RED: 3 expected fork-source failures / 1 upstream pass. GREEN and focused identity suite: 3 files / 9 tests; `tsc --noEmit`: pass |
| `bd76c63` | Crew session-model inheritance | Adapted | Cherry-picked as `d228eb7` with upstream authorship and `-x` trailer; review follow-up `d1ce9d2` routes reset guards through `task.*`, applies selected model precedence to new task-bound lobby workers, and derives host models from current Pi context with a documented saved-state fallback. | RED: 5 focused expected failures; GREEN: 12 focused Team/Superpowers/model files / 109 tests; `tsc --noEmit` and `git diff --check`: pass |
| `a97fbf2` | Compact Crew event artifacts | Adapted | Cherry-picked as `ecd4251` with `-x` trailer and auto-merged without a Git conflict. Scoped runtime review found that real Pi terminal assistant provider errors arrive as `message_update`/`message_end` events, not only as the synthetic event covered upstream; follow-up `65d9642` detects those terminal events while retaining the established synthetic compatibility path. Team/model/Superpowers launch boundaries remain unchanged. | Focused artifact/launch: 5 files / 28 tests; `tsc --noEmit` and `git diff --check`: pass |
| `2e83f14` | Bounded feed reads and task snapshots | Adapted | Cherry-picked intact as `c4efdeb` with upstream authorship and `-x` trailer; `f5acbcf` reloads the render-local task snapshot after mutations; review round 1 `fe07c36` makes reload conditional on an explicit mutation result and orders it before later task checks; review round 2 `bbd8cfd` adds the `SpawnResult.mutated` signal for failed lobby task mutations | RED: 2 expected overlay failures; GREEN: overlay 4/4; focused feed/render/Team/status: 6 files / 51 tests; `tsc --noEmit`: pass; round-one full suite: 58 files / 699 tests; Task 8 final full suite: 58 files / 701 tests |
| `2f5e7dc` | v0.15.0 release metadata | Adapted | Cherry-picked as `687083e` with upstream authorship and `-x` trailer. Per the explicit user decision, set the fork's package and lockfile root versions to `0.15.0`; retained every fork identity field, dependency model/floors, Node floor, and README branding. The changelog closes the fork's prior Unreleased work, credits upstream `pi-messenger` v0.15.0, and explicitly says this was not a publication or release. | `npm ci`; TypeScript; branding/installer: 2 files / 7 tests; metadata, package dry run, and diff checks: pass |

## Conflict Decisions

- `2c3b748` `.gitignore` content conflict: retained the fork's `node_modules/`, `npm-shrinkwrap.json`, `*.DS_Store`, `.pi/`, and `.worktrees/` rules; retained upstream's `# Local agent scratch` and `progress.md`; did not restore `package-lock.json`, because this fork intentionally commits its lockfile.
- `2c3b748` `CHANGELOG.md` content conflict: retained the fork's existing `Unreleased / Added` Superpowers authorization entry and added upstream's optional Team-layer entry below it. No prior history was replaced.
- No other files conflicted. Conflict-marker scan found none after resolution; `git diff --check` passed.
- `8d24757` produced content conflicts in `config-overlay.ts`, `crew/agents.ts`, `crew/handlers/plan.ts`, `crew/handlers/review.ts`, `crew/handlers/status.ts`, `crew/handlers/sync.ts`, `crew/handlers/task.ts`, `crew/handlers/team.ts`, `crew/handlers/work.ts`, `crew/index.ts`, `index.ts`, `overlay-actions.ts`, `tests/crew/status.test.ts`, `tests/crew/team-routing.test.ts`, `tests/crew/team-task-approval.test.ts`, `tests/crew/worker-coordination.test.ts`, `tests/mention-autocomplete.test.ts`, and `tsconfig.json`.
- Mechanical `8d24757` resolutions accepted current-Pi `.ts` import suffixes, explicit `ctx.cwd` flow, and combined TypeScript options (`allowImportingTsExtensions` plus the fork's Node types). Both upstream and fork test cases were retained where adjacent additions conflicted.
- Meaningful `8d24757` resolutions were dictated by approved constraints: retained Superpowers selection/provenance/child guard/native fallback in `crew/agents.ts`; retained outer Superpowers policy and loaded-skill capture in `index.ts`; retained controller-only approval guards in both router and handler; kept worker coordination free of approval/rejection commands while adding rejected-task revision guidance; kept the post-role Crew-only restriction and bounded memory behavior; and exposed Team status before fork-owned Superpowers status.
- `8d24757` package metadata auto-merged upstream peer-dependency wildcards. The resolution restored the fork manifest unchanged: `super-pi-messenger@0.14.1`, `typebox` runtime floor `^1.1.24`, current `@earendil-works` development floors, Node `>=24`, repository metadata, and package manager. No package-lock change was made.
- `bd76c63` auto-merged all 18 changed files and created `d228eb7d86232d02a4962e4a96fdc2a3e2ea69ea`; `git diff --name-only --diff-filter=U` was empty. No conflict resolution was performed. User decisions: retain upstream model precedence exactly—task → request parameter → Team role → Crew config → host session → agent frontmatter—and have newly spawned warm lobby workers inherit the initiating host session model when no stronger override exists. Already-running warm workers retain their launch-time model.
- `a97fbf2` auto-merged `CHANGELOG.md`, `README.md`, and `crew/agents.ts` into `ecd4251`; the cherry-pick did not pause, so no Git conflict was resolved. A later scoped runtime review found a composition gap rather than a merge conflict: upstream coverage modeled only a synthetic `provider_error` object with nested numeric status, while Pi JSON mode reports real terminal provider failures on assistant `message_update` and `message_end` events with `stopReason: "error"` and `errorMessage` that may begin with formatted status text such as `400: ...`. Adaptation `65d9642` adds that realistic terminal-event handling without broadening detection to tool output, arbitrary assistant text, or nonterminal events.

## Checkpoint Results

### `2c3b748` Optional Crew Team Foundation

- Cherry-picked with `git cherry-pick -x 2c3b748` as `ad4fb911f37201d06232d874de30961c3923ccde`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-05-02 23:51:01 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 01:08:05 -0500`).
  - Preserved trailer: `(cherry picked from commit 2c3b748559de829207a191240c166788e4f3577f)`.
  - Import result: 37 files changed; 2,447 insertions and 110 deletions after the required conflict resolutions.
- Fork-only compatibility adaptation: the imported `crew/handlers/team.ts` referenced `@mariozechner/pi-coding-agent`, absent from this fork's manifest and `node_modules`. The existing fork uses `@earendil-works/pi-coding-agent`; follow-up `32d5e2d1fc8dd65ebfecfba0ccf9f6ec01cd1fd8` changes that type-only import. This was required because the prescribed `npm exec tsc -- --noEmit` first failed with `TS2307`; it passed after the one-line adaptation.
- Authorization/execution-boundary review: Team code discovers role metadata and routes all work through existing `crew/spawn.ts`, `crew/lobby.ts`, `crew/handlers/work.ts`, `index.ts`, and `overlay.ts` Crew paths. `crew/handlers/team.ts` does not spawn or launch a subagent; it explicitly states that Team roles are labels/prompt context and Crew remains the execution engine. No new launch path was found.
- Verification after the adaptation:
  - `npm exec vitest -- run tests/crew/team-overlay-render.test.ts tests/crew/team-routing.test.ts tests/crew/team-store.test.ts tests/crew/team-subagent-roles.test.ts tests/crew/team-task-approval.test.ts tests/crew/team-work.test.ts tests/crew/worker-coordination.test.ts` — exit `0`; 7 files and 61 tests passed.
  - `npm exec vitest -- run tests/crew/superpowers.test.ts tests/crew/superpowers-policy.test.ts tests/crew/superpowers-guard.test.ts tests/crew/superpowers-launch.test.ts tests/crew/superpowers-extension.test.ts` — exit `0`; 5 files and 58 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `npm test` — exit `0`; 51 files and 651 tests passed.

### `8d24757` Current-Pi Team Workflows

- Cherry-picked with `git cherry-pick -x 8d24757` as `f58620ed1d723b84824e41897eaf68c418b72345`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:33:17 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 01:38:41 -0500`).
  - Preserved trailer: `(cherry picked from commit 8d24757494064770411d23997f73bff61d8498a3)`.
  - Import result: 76 files changed; 966 insertions and 608 deletions after conflict composition.
- Semantic gate decisions:
  - Team setup, profile, charter, memory, roles, approvals, revisions, work, and overlays continue through existing Crew routes; Team handlers do not introduce a second launcher.
  - `crew/agents.ts` retains fork-owned Superpowers selection, loaded-skill provenance, child guard extension/environment, role metadata, ordered guidance, and native fallback.
  - Team role/charter/memory context stays in the Crew task prompt. The higher-priority Crew-only restriction remains after imported Team text. Superpowers system guidance remains fork-owned and is not duplicated by Team context.
  - Pending approval and rejected revision states remain in `.pi/messenger/crew/` task records. Crew child markers cannot approve or reject. Rejected tasks are reported separately and return to work only through revision actions.
  - Overlay actions use existing Crew task and spawn controls; no new unreviewed dispatch path was added.
  - Fork package identity and dependency floors were retained unchanged.
- RED/GREEN adaptation evidence:
  - Added `tests/crew/status.test.ts` coverage requiring active Team details and fork-owned Superpowers details in one Crew status response, with Team displayed before Superpowers.
  - RED: `npm exec vitest -- run tests/crew/status.test.ts` exited 1 with 1 expected failure because `## Team` and `details.team` were absent.
  - GREEN: the same command exited 0 with 1 file / 11 tests after the smallest status composition.
  - Conflict-composition focus then passed: 8 files / 83 tests.
- Checkpoint A verification:
  - Team focus command: exit 0; 12 files / 160 tests.
  - Superpowers boundary command: exit 0; 7 files / 62 tests.
  - `npm exec tsc -- --noEmit`: exit 0; no output.
  - `npm test`: exit 0; 51 files / 663 tests.
  - Final controller diagnostics (recorded below) supersede the earlier tool-availability note: explicit scans covered the relevant changed TypeScript paths with 0 diagnostics.
  - Conflict-marker scan, `git diff --check`, and package identity checks passed before cherry-pick continuation.

### Boundary-hardening adaptation (`c3eb4fe`)

- User decision: task approval and rejection are controller-only. Every Crew child process is denied, and worker coordination text no longer provides approval or rejection commands.
- User decision: a higher-priority Crew-only restriction is appended after imported Team role text, so imported roles cannot override Crew execution boundaries.
- Correctness adaptation: Team profile `memory.maxCharsPerType` must be a positive, bounded integer (1 through 10,000); Team memory entries that exceed the per-type prompt budget are skipped, including an oversized first entry.
- Adaptation commit: `c3eb4feda32629f881ca2b9e948ce5db8cd4d7c8 fix: harden Crew Team approval boundaries`.
- TDD evidence: the new boundary tests first failed (RED) for all three review findings, then passed after the adaptation (GREEN). The focused boundary result was `13` relevant Team/Superpowers test files and `134` tests passed; `npm exec tsc -- --noEmit` passed.
- Task review result: all three findings were addressed. Review found no new Critical or Important breakage.

### Crew status hardening adaptation (`356aa55`)

- Adaptation commit `356aa55` separates executable, pending-approval, and rejected tasks in Crew status.
- Only executable tasks receive ready/work guidance; pending receives approval guidance; rejected receives revision guidance.
- Changelog packaging text was corrected to match typebox runtime and Pi development dependencies.
- TDD evidence: RED status suite 3 expected failures / 11 passes; GREEN 14/14; Team/status 163/163; Superpowers 62/62; TypeScript and diff check passed.
- Scoped re-review found both issues addressed and no new Critical/Important breakage.

### `5cb1f53` Backlog Compatibility Fixes

- Cherry-picked intact with `git cherry-pick -x 5cb1f53` as `67c994f4ded42969a91e5731f943477bab388143`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:49:50 -0700`).
  - Preserved trailer: `(cherry picked from commit 5cb1f53449b8a3d49d6396f7dad003fa66409e5b)`.
  - Import result: 5 files changed; 163 insertions and 10 deletions.
- Git auto-merged `CHANGELOG.md` and `index.ts`; the cherry-pick did not pause and required no conflict resolution. Direct review confirmed `workerCap = Math.min(maxWorkers, config.concurrency.max)` limits both lobby assignment and new worker spawning, while existing Team approval filtering and prompt context remain in both paths.
- Direct review also confirmed `StringEnum` probes optional `Type.Unsafe` and otherwise uses `Type.Union(values.map(Type.Literal), options)`. Fork Superpowers imports and lifecycle wiring remain present in `index.ts`; no package identity or dependency metadata changed.
- Imported upstream test evidence was retained. No fork adaptation or bug fix was needed, so no additional RED/GREEN cycle applied.
- Verification:
  - `npm exec vitest -- run tests/crew/spawn.test.ts tests/typebox-compat.test.ts tests/crew/worker-coordination.test.ts tests/crew/superpowers-extension.test.ts` — exit `0`; 4 files and 37 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `git diff --check` — exit `0`; no output.
- Self-review found no Critical or Important issue. Controller-only approvals, Team gating, fork lifecycle imports, Superpowers wiring, and package identity are unchanged by this import.

### `bd76c63` Crew Session-Model Inheritance

- Cherry-picked with `git cherry-pick -x bd76c63` as `d228eb7d86232d02a4962e4a96fdc2a3e2ea69ea`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:56:47 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 02:03:03 -0500`).
  - Preserved trailer: `(cherry picked from commit bd76c6381c294b774148674adbdbda3cc8f42872)`.
  - Git auto-merged all 18 files; no conflicted paths remain.
- User decisions: retain upstream model precedence exactly—task → request parameter → Team role → Crew config → host session → agent frontmatter. Newly spawned warm lobby workers inherit the initiating host session model when no stronger override exists; already-running warm workers retain their launch-time model.
- Code review confirms `resolveModel` implements that exact order. The work path supplies task, request, Team role, Crew config, and session inputs; planner, reviewer, analyst/sync, and revision pass Crew-config-or-session overrides to the common launcher; and a new lobby process resolves worker config → host session → `crew-worker` frontmatter. Assigning an existing lobby process does not relaunch it, so it retains its launch-time model.
- Tests state the exact resolver sequence and the host-session work fallback. The focused suite also covers `--provider`/`--model` splitting, plain models, `.js` extension entries, Windows `pi.cmd`, duplicate-active-worker guards, Team behavior, and the retained Superpowers launch boundary.
- Fork Superpowers launch composition remains intact in `crew/agents.ts`: worker/reviewer launches still append the guard extension and child environment marker. The lobby launcher had no Superpowers guard before or after this import; this commit only changes its executable to `pi.cmd` on Windows and its model fallback.
- No fork adaptation was needed, so no RED/GREEN follow-up production commit was created.
- Verification:
  - `npm exec vitest -- run tests/crew/model-override.test.ts tests/crew/superpowers-launch.test.ts tests/crew/lobby.test.ts tests/crew/task-actions.test.ts tests/crew/team-work.test.ts` — exit `0`; 5 files / 65 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `git diff --check` — exit `0`; no output.
- Self-review: no Critical or Important issue found. This check did not add a dedicated integration test that observes an existing lobby process retain its model; that behavior follows from assignment using the registered process without respawn, and the new-process fallback is directly visible in `spawnLobbyWorker`.

### `bd76c63` Review Fix Round 1

- Fork adaptation commit `d1ce9d2` (`fix: reconcile Crew model routing and task guards`) addresses all three Important review findings without changing controller-only approvals, Team approval filtering, role containment, Superpowers guard/provenance, Windows `pi.cmd`, fork identity, or imported launch behavior.
- RED: `npm exec vitest -- run tests/crew/router-status.test.ts tests/crew/lobby.test.ts tests/crew/model-routing.test.ts` exited `1`; five focused failures showed that the `pi_messenger` `task.reset` route omitted the active-worker predicate, new task-bound lobby workers selected Crew config ahead of task/request/Team-role values, and action routing used stale `state.model` instead of the switched host model.
- GREEN: the same focused command exited `0`; 3 files / 37 tests passed. The reset handler now passes `hasActiveWorker` to `executeTaskAction`, so both direct and cascade reset use the existing active-worker protection.
- New task-bound lobby processes now resolve task → request (where supplied) → Team role → Crew config → host session → agent frontmatter. The launch receives the resolved stronger value; unassigned and already-running lobby workers retain their prior generic launch behavior and are only assigned, never relaunched.
- Host model routing now turns current Pi context `{ provider, id }` into `provider/model` at each action, retaining `pushModelArgs` splitting into `--provider` and `--model`. Saved `state.model` is used only when `ctx.model` is absent; this fallback is documented in `README.md`.
- Verification: `npm exec vitest -- run tests/crew/model-override.test.ts tests/crew/model-routing.test.ts tests/crew/router-status.test.ts tests/crew/lobby.test.ts tests/crew/spawn.test.ts tests/crew/task-actions.test.ts tests/crew/team-work.test.ts tests/crew/team-routing.test.ts tests/crew/team-task-approval.test.ts tests/crew/superpowers-launch.test.ts tests/crew/superpowers-guard.test.ts tests/crew/superpowers-policy.test.ts` — exit `0`; 12 files / 109 tests. `npm exec tsc -- --noEmit` — exit `0`. `git diff --check` — exit `0`.
- Self-review: no Critical or Important issue found. The new route test exercises the actual `executeCrewAction` `task.reset` path; the model matrix checks each required new-worker source and a provider-qualified host ID. Existing spawn/Team tests confirm warm-worker assignment remains non-relaunching and approval-gated tasks remain filtered. The optional request-model parameter is only consumed by callers that have a request model; existing overlay calls preserve their prior argument order and behavior.

### `a97fbf2` Compact Crew Event Artifacts

- Cherry-picked with `git cherry-pick -x a97fbf2` as `ecd4251e6dead53a1ee67df65e283f8f21084f85`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:59:24 -0700`).
  - Preserved trailer: `(cherry picked from commit a97fbf2cc41e4d5c90fb11acfe4fe77dad7efbe2)`.
  - Git auto-merged `CHANGELOG.md`, `README.md`, and `crew/agents.ts`; the pick did not pause and required no conflict resolution.
- The streaming path retains the latest final assistant text, while `compactEventForArtifact` removes repeated `message_update` snapshots before JSONL append. `updateProgress` remains before compaction, so tool progress, tokens, and message errors are retained. Output files and metadata retain final output, exit code, duration, token total, truncation, and error values.
- Root cause from scoped runtime review: the imported provider fail-fast coverage handled only a synthetic `provider_error` event with nested numeric status. Real Pi JSON-mode provider failures arrive as terminal assistant `message_update` or `message_end` events with `message.stopReason: "error"` and `message.errorMessage`; formatted HTTP status may be a leading string such as `400: ...`. Those real events were not recognized, so `runAgent` did not fail fast.
- Fork adaptation `65d9642` (`fix: detect terminal Crew provider errors`) recognizes terminal assistant error events plus the established synthetic compatibility event, derives only the allowlisted 4xx status from structured data or a leading formatted `errorMessage`, records the error, and forces exit code `1`. It does not inspect tool results, arbitrary tool output, ordinary assistant text, or nonterminal events.
- Fork launch composition remains in `crew/agents.ts`: Team role/model behavior, Superpowers guidance, guard extension, and child environment marker are outside the event-processing changes.
- Verification:
  - RED: realistic terminal assistant `message_update` and `message_end` quota-error regressions failed as expected because neither caused `SIGTERM`.
  - GREEN: `npm exec vitest -- run tests/crew/agent-events.test.ts tests/crew/utils/artifacts.test.ts tests/crew/live-progress.test.ts tests/crew/graceful-shutdown.test.ts tests/crew/superpowers-launch.test.ts` — exit `0`; 5 files / 28 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `git diff --check` — exit `0`; no output.
- Scoped re-review verdict: the runtime finding is addressed. Compaction, final-output, progress/token, artifact/metadata, retention, Team/model, and Superpowers boundaries remain intact; no new Critical or Important breakage was found.

### `2e83f14` Bounded Feed Reads and Render Snapshots

- Cherry-picked intact with `git cherry-pick -x 2e83f14` as `c4efdeb6bfb808b485a0de6e3c84f0c6df2a22d7`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 15:44:28 -0700`).
  - Preserved trailer: `(cherry picked from commit 2e83f14287a604c5ccb6e4bcab271667bb58223d)`.
  - Git auto-merged the seven imported files without pausing; no Git conflict was resolved.
- The imported bounded-tail reader keeps the public `readFeedEvents(cwd, limit)` contract: results remain in append order, malformed JSONL records are skipped, a missing final newline is read, feeds stay project-scoped, and existing Crew-event filtering stays at the caller boundary. The upstream tests add bounded cross-chunk and non-positive-limit coverage.
- Fork adaptation `f5acbcf` (`fix: reload Crew tasks after render mutations`) corrects a composition issue found in review: auto-spawn or auto-refill can persist task changes after the first render-local snapshot has been read. `MessengerOverlay.render` now reloads tasks after those mutation-capable checks, reclamps selection, and passes that one fresh snapshot through status, task list/summary/detail selection, completion, and agent-row rendering for the rest of the frame. It does not cache task data across frames or callbacks.
- Strict TDD evidence for the adaptation:
  - RED: `npm exec vitest -- run tests/overlay.test.ts` exited `1` with the expected stale-frame failure: after mocked auto-spawn persisted `in_progress`, the rendered frame still displayed `○ task-1` rather than `● task-1 … (WorkerOne)`.
  - GREEN: the same command exited `0`; 1 file / 1 test passed after the minimal reload.
- Checkpoint C verification:
  - `npm exec vitest -- run tests/feed.test.ts tests/crew/live-feed.test.ts tests/overlay-coordinator.test.ts tests/crew/team-overlay-render.test.ts tests/crew/status.test.ts tests/overlay.test.ts` — exit `0`; 6 files / 48 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `npm test` — exit `0`; 58 files / 696 tests passed.
  - `git diff --check` and `git show --check` for `c4efdeb` and `f5acbcf` — exit `0`; no output.
  - Final controller diagnostics (recorded below) supersede the earlier tool-availability note: explicit scans covered the relevant changed TypeScript paths with 0 diagnostics.
- Self-review: no Critical or Important issue found. The import does not change Team/Superpowers combined status, Team approval gating, current model routing, provider-error compaction, overlay coordinator behavior, or fork package identity. The follow-up only makes the mutation boundary explicit and leaves all post-mutation rendering on a fresh, single-frame snapshot.

### `2e83f14` Review Fix Round 1

- Root cause: `f5acbcf` unconditionally read the task directory a second time after both automatic checks. That avoided a stale rendered frame but meant an ordinary render read twice, and `checkAutoRefillWorkers` still received the stale pre-auto-spawn snapshot.
- Trace: `render()` reads tasks, `checkAutoSpawnOnPlanComplete` derives ready tasks from that array and can call `spawnWorkersForReadyTasks`, and then `checkAutoRefillWorkers` derives its in-progress/ready state from the same old array. `spawnWorkersForReadyTasks` persists assignments through task-store updates, including `status: "in_progress"`; its `assigned` result is therefore the mutation boundary. Status, completion, selection, detail/list/summary, and agent-row helpers consume the eventual render snapshot.
- Adaptation commit `fe07c36` (`fix: order overlay task snapshot reloads`) changes both mutation-capable checks to return whether `assigned > 0`. `render()` reloads immediately after each true result, before the later auto-refill task-dependent check and before rendering. When neither check mutates, all helpers consume the single original snapshot.
- Strict TDD evidence:
  - RED: `npm exec vitest -- run tests/overlay.test.ts` exited `1` with two expected failures: an ordinary render called `getTasks` twice, and auto-refill received `["todo", "todo"]` rather than the post-auto-spawn `["in_progress", "todo"]` snapshot.
  - The auto-refill same-frame rendering test was already green under the prior unconditional reload; it remains to protect the new conditional reload path.
  - GREEN: `npm exec vitest -- run tests/overlay.test.ts` — exit `0`; 1 file / 4 tests passed.
- Verification: `npm exec vitest -- run tests/feed.test.ts tests/crew/live-feed.test.ts tests/overlay-coordinator.test.ts tests/crew/team-overlay-render.test.ts tests/crew/status.test.ts tests/overlay.test.ts` — exit `0`; 6 files / 51 tests. `npm exec tsc -- --noEmit` — exit `0`. `npm test` — exit `0`; 58 files / 699 tests. `git diff --check` — exit `0`.
- Self-review: the mutation result follows the existing `spawnWorkersForReadyTasks` persistence result; no Team approval filtering, combined Team/Superpowers status, feed behavior, or render-helper task fallback changed. The separate missing-final-newline coverage suggestion is deferred by instruction.
- Review state: this round addresses the reported Important stale-snapshot and unconditional-read finding. No new Critical or Important concern was found in self-review.

### `817e1ae` Legacy Installer Package-Conflict Guard

- Cherry-picked intact with `git cherry-pick -x 817e1ae` as `5dc727a69b4b278c62b20f64920c88f8ec96c1f8`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:51:49 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 01:58:19 -0500`).
  - Preserved trailer: `(cherry picked from commit 817e1ae000f3119d46d2d7a5c7656c86ee58e59a)`.
  - Git auto-merged `CHANGELOG.md`; the pick did not pause and required no conflict resolution. No conflict markers were introduced and `git diff --check` passed.
- Strict TDD fork adaptation: changed the imported single-source test to parameterize exactly `npm:pi-messenger`, `npm:super-pi-messenger`, `git:github.com/aeturnal/super-pi-messenger`, and `git:github.com/aeturnal/super-pi-messenger@main` before changing `install.mjs`.
  - RED: `npm exec vitest -- run tests/install-cli.test.ts` exited `1`: upstream npm passed, while the three fork sources failed because no refusal output was emitted and the installer created the legacy copy.
  - GREEN: the same command exited `0`; 1 file / 4 tests passed after the smallest detection change.
- Fork adaptation commit `98e7ec065a790eda96d927793a53df1518e03152` (`fix: adapt native install guard for fork identity`) checks only `~/.pi/agent/npm/node_modules/pi-messenger` and `super-pi-messenger`, exact upstream/fork npm sources, and the specified fork git source with an optional nonempty `@ref`. It keeps `extensions/pi-messenger` and `pi-messenger` intact. Fork refusals say `Super Pi Messenger` and explain that both copies register `pi_messenger`.
- Verification:
  - Final controller diagnostics (recorded below) supersede the earlier tool-availability note: explicit scans covered the relevant changed TypeScript paths with 0 diagnostics.
  - `npm exec vitest -- run tests/install-cli.test.ts tests/crew/utils/install.test.ts tests/readme-branding.test.ts` — exit `0`; 3 files / 9 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `git diff --check` — exit `0`; no output.
  - Package/repository/CLI identity check — exit `0`: `super-pi-messenger`, `git+https://github.com/aeturnal/super-pi-messenger.git`, and `pi-messenger: install.mjs` are retained.
- Self-review: no critical or important issue found. Detection is intentionally not broadened beyond the listed npm directory/source forms and the exact fork git URL with optional ref; Team and Superpowers files were not changed.

### `2e83f14` Review Fix Round 2

- Fork adaptation commit `bbd8cfd` (`fix: report failed lobby task mutations`) adds the explicit `SpawnResult.mutated` signal. Failed lobby delivery reports `mutated: true` when task-store start metadata was persisted, even though `assigned: 0`; the render reloads the task snapshot, while notifications still do not claim that a worker started.
- Root cause: the round-1 checks used `assigned > 0` as their mutation signal. In the warm-lobby path, `spawnWorkersForReadyTasks` first persists `status`, `started_at`, `base_commit`, `assigned_to`, and incremented `attempt_count`; when assignment delivery fails, it restores only `status` and `assigned_to`. The persisted start metadata therefore changes while the function reports `assigned: 0`, so the render retains its earlier task snapshot and detail output can show the old attempt count.
- Strict TDD evidence: `npm exec vitest -- run tests/crew/spawn.test.ts tests/overlay.test.ts` exited `1` before the production change. The new spawn regression received `mutated: undefined` rather than `true`, and the detail-frame regression rendered `Status: todo │ Attempts: 0` rather than `Attempts: 1`.
- Adaptation: `SpawnResult` now includes the explicit `mutated` boolean. The spawn path marks it before a lobby task-store update and after a successful non-lobby worker start. Auto-spawn and auto-refill return this signal to `render()`, which reloads only after a reported mutation. Notifications still depend on `assigned`, so a failed lobby delivery does not claim a worker started. Attempt-count and partial-restore behavior are unchanged.
- Regression coverage: the spawn test proves failed lobby assignment reports mutation while retaining `started_at`, `base_commit`, and incremented `attempt_count` after status/assignee restoration. The overlay detail regression proves that an `assigned: 0, mutated: true` result reloads before detail output. The existing ordinary one-read and successful auto-spawn/auto-refill same-frame tests remain intact.
- GREEN and verification:
  - `npm exec vitest -- run tests/crew/spawn.test.ts tests/overlay.test.ts` — exit `0`; 2 files / 9 tests passed.
  - `npm exec vitest -- run tests/overlay.test.ts tests/crew/spawn.test.ts tests/feed.test.ts tests/crew/live-feed.test.ts tests/overlay-coordinator.test.ts tests/crew/team-overlay-render.test.ts tests/crew/status.test.ts` — exit `0`; 7 files / 56 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `npm test` — exit `0`; 58 files / 701 tests passed.
  - `git diff --check` — exit `0`; no output.
- Self-review: no Critical or Important concern found. The boolean reports task-store mutation rather than assignment success, avoids an unconditional second read on ordinary or reported-no-op frames, and preserves Team approval filtering, Team/Superpowers status, feed behavior, and render helper fallbacks.

### `2f5e7dc` Fork v0.15.0 Metadata

- The user explicitly selected fork version `0.15.0`; this is local repository metadata only, not authorization to publish or release.
- Cherry-picked with `git cherry-pick -x 2f5e7dc` as `687083e9077c6124075557c18e5103ec0b7d1181`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 15:54:12 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 03:05:17 -0500`).
  - Preserved trailer: `(cherry picked from commit 2f5e7dc9c77fd7a3fba4728931e8564ce48d9bab)`.
- Intentional upstream deviations retained: fork name, description, author, repository/homepage/bugs, technical `pi-messenger` bin, Node `>=24`, TypeBox runtime dependency, current `@earendil-works`/TypeScript/Vitest development floors, files, keywords, `pi`, and `packageManager`. Upstream peer-dependency wildcards and identity fields were not adopted.
- `CHANGELOG.md` closes fork Unreleased work as Super Pi Messenger `0.15.0` on `2026-08-04`, preserves fork entries and upstream contributor credits, credits incorporation of upstream `pi-messenger` v0.15.0, and explicitly says no publication or release occurred.
- `npm install --package-lock-only --ignore-scripts` changed only package-lock root and root-package versions from `0.14.1` to `0.15.0`; no dependency range or resolution drift occurred. `npm ci` exited `0` (191 packages added; npm reported 3 vulnerabilities and install-script approval warnings for `@google/genai@1.52.0` and `protobufjs@7.6.5`).
- Verification:
  - `npm exec tsc -- --noEmit` — exit `0`; no output. No TypeScript file changed from Checkpoint C through this metadata task. Final controller diagnostics (recorded below) explicitly covered the relevant changed TypeScript paths with 0 diagnostics.
  - `npm pkg get name version repository homepage bugs` — confirmed `super-pi-messenger@0.15.0` and fork-only URLs.
  - `npm exec vitest -- run tests/readme-branding.test.ts tests/install-cli.test.ts` — exit `0`; 2 files / 7 tests passed.
  - `npm pack --dry-run` — exit `0`; package `super-pi-messenger@0.15.0`, 62 intended files.
  - `git diff --check`, `git diff --cached --check`, and `git show --check HEAD` — exit `0`.
- Self-review found no Critical or Important issue: identity, dependency floors, README identity, installer technical name, attribution, lockfile consistency, authorship, and `-x` trailer are retained. No publication, release, push, merge, or package upload occurred.

## Remaining Risks

The model-precedence and warm-lobby decisions are resolved. Remaining risks: project-local agent frontmatter can still set a model when every stronger source is absent, and an existing lobby process intentionally keeps its launch-time model even if the host session model later changes. Final explicit diagnostics cover all 87 integration-changed TypeScript paths with 0 diagnostics, but a capped full-worktree scan reports pre-existing diagnostics in untouched eval tests; equivalent diagnostics reproduce on fork `main`, so this report does not claim the whole historical repository is LSP-clean. The reviewed test-typing adaptations `d030db9`, `fa4aa63`, `2845b6c`, `2ca823b`, and `d4f341e` changed no production behavior. npm continues to report 3 existing dependency vulnerabilities and two install-script approval warnings; neither was changed by this metadata task.

## Final Audit and Review Handoff

Date: `2026-08-04T03:12:21-05:00` (local audit start; all commands below were run fresh in this integration worktree).

### Commit, author, and file accounting

- Trailer/author audit command:
  `for c in 2c3b748 8d24757 5cb1f53 817e1ae bd76c63 a97fbf2 2e83f14 2f5e7dc; do full=$(git rev-parse "$c"); git log 3315090..HEAD --format='%H%n%an <%ae>%n%B' | grep -F "cherry picked from commit $full"; done` — exit `0`; exactly one full-hash `-x` trailer was found for each source commit.
- The attributed integration commits are `ad4fb91` (`2c3b748`), `f58620e` (`8d24757`), `67c994f` (`5cb1f53`), `5dc727a` (`817e1ae`), `d228eb7` (`bd76c63`), `ecd4251` (`a97fbf2`), `c4efdeb` (`2e83f14`), and `687083e` (`2f5e7dc`). Each preserves upstream author `Nico Bailon <nico.bailon@gmail.com>` and its matching full-hash trailer. The detailed commit ledger and each reviewed fork adaptation are recorded above.
- `git diff --name-status f836c400103bb3c99ff350aa9c3d0e80e58f046b..upstream/main` reported `92` upstream paths: `49` non-test paths and `43` test paths. `git diff --name-status 3315090..HEAD` reported `98` integration paths. The integration-only paths are the fork adaptation helper `crew/utils/child-process.ts`, the design/plan/report documents, `package-lock.json`, and focused fork tests `tests/crew/model-routing.test.ts` and `tests/overlay.test.ts`.
- Every non-test upstream path is classified below. `overlay-coordinator.ts` is retained: it is upstream-changed relative to the shared base but `git diff 3315090..upstream/main -- overlay-coordinator.ts` is empty, so the fork already contained the equivalent behavior before this integration.

| Disposition | Paths | Accounting |
|---|---|---|
| Retained (`29`) | `config-overlay.ts`, `config.ts`, `crew/handlers/coordination.ts`, `crew/handlers/revise.ts`, `crew/index.ts`, `crew/live-progress.ts`, `crew/prompt.ts`, `crew/registry.ts`, `crew/state-autonomous.ts`, `crew/state-planning.ts`, `crew/state.ts`, `crew/team/store.ts`, `crew/team/subagent-roles.ts`, `crew/team/types.ts`, `crew/types.ts`, `crew/utils/config.ts`, `crew/utils/discover.ts`, `crew/utils/progress.ts`, `crew/utils/risk-labels.ts`, `crew/utils/task-format.ts`, `crew/utils/team-roles.ts`, `feed.ts`, `handlers.ts`, `overlay-actions.ts`, `overlay-coordinator.ts`, `overlay-render.ts`, `skills/pi-messenger-crew/SKILL.md`, `store.ts`, `tsconfig.json` | Upstream behavior is present; `overlay-coordinator.ts` was already equivalent as noted above. No incompatible production path exists. |
| Adapted (`20`) | `.gitignore`, `CHANGELOG.md`, `README.md`, `crew/agents.ts`, `crew/handlers/plan.ts`, `crew/handlers/review.ts`, `crew/handlers/status.ts`, `crew/handlers/sync.ts`, `crew/handlers/task.ts`, `crew/handlers/team.ts`, `crew/handlers/work.ts`, `crew/lobby.ts`, `crew/spawn.ts`, `crew/store.ts`, `crew/task-actions.ts`, `crew/utils/artifacts.ts`, `index.ts`, `install.mjs`, `overlay.ts`, `package.json` | The exact non-identical composition is documented in the commit ledger and conflict decisions: fork package/import identity; Superpowers launch, provenance, and child containment; controller-only approval/revision and bounded Team memory; model precedence/current host model/reset guards; terminal provider-error artifacts; mutation-aware overlay snapshots; legacy installer source detection; and fork v`0.15.0` metadata/branding. `.gitignore`, changelog, and README retain both fork history/branding and relevant upstream documentation. |

All `43` changed upstream test paths are retained at `HEAD`: the audit commands `git diff --name-status f836c400103bb3c99ff350aa9c3d0e80e58f046b..upstream/main -- 'tests/**'` and a `test -e` check for every listed path reported `43` paths, `0` missing, and `0` deleted or renamed. None was omitted, deleted, or replaced. Fork-specific adaptations amend retained tests in place; separate fork tests were added or extended for policy/approval containment, model routing and reset guards, realistic terminal provider errors/artifacts, failed-lobby mutation snapshots, installer identity, package branding, and TypeBox fallback. The final test-typing adaptations are `d030db9` (lobby fixtures), `fa4aa63` (spawn nullable mock), `2845b6c` plus `2ca823b` (typed details assertions and feedback exactness), and `d4f341e` (AgentProgress fixtures); they changed no production behavior.

### Final verification evidence

Commands run (in order after the trailer/file audit):

```bash
npm exec tsc -- --noEmit
npm exec vitest -- run tests/crew/team-overlay-render.test.ts tests/crew/team-routing.test.ts tests/crew/team-store.test.ts tests/crew/team-subagent-roles.test.ts tests/crew/team-task-approval.test.ts tests/crew/team-work.test.ts tests/crew/superpowers.test.ts tests/crew/superpowers-policy.test.ts tests/crew/superpowers-guard.test.ts tests/crew/superpowers-launch.test.ts tests/crew/superpowers-extension.test.ts tests/crew/model-override.test.ts tests/crew/agent-events.test.ts tests/crew/utils/artifacts.test.ts tests/feed.test.ts tests/install-cli.test.ts tests/typebox-compat.test.ts tests/readme-branding.test.ts
npm ci
npm exec tsc -- --noEmit
npm test
npm exec vitest -- run tests/evals
git diff --check
npm pack --dry-run
npm pkg get name version repository homepage bugs
git status --short --branch
```

- Final controller diagnostics supersede the earlier availability note. Explicit scans found 0 diagnostics in `crew/` (41 files), `tests/crew/` (40 files), and 19 changed root/test paths. These scans cover all 87 integration-changed TypeScript paths.
- `npm exec tsc -- --noEmit` — exit `0` before focused testing and again after fresh `npm ci`; no output.
- Focused integration command from Task 10 — exit `0`; `18` files and `135` tests passed.
- `npm ci` — exit `0`; `191` packages added and `192` audited. npm reported `3` pre-existing dependency vulnerabilities (`1` moderate, `2` high) and allow-scripts warnings for `@google/genai@1.52.0` and `protobufjs@7.6.5`.
- `npm test` — exit `0`; `58` files and `701` tests passed. Baseline was `45` files and `607` tests.
- `npm exec vitest -- run tests/evals` — exit `0`; `8` files and `197` deterministic eval definition, fixture, reset, and verifier-contract tests passed. No live model-backed eval was started; it remains not run because no approval was given and it may consume provider credits.
- `git diff --check` — exit `0`.
- `npm pack --dry-run` — exit `0`; `super-pi-messenger@0.15.0`, `62` intended files, package size `166.3 kB`, unpacked size `662.2 kB`.
- `npm pkg get name version repository homepage bugs` — exit `0`; confirmed `super-pi-messenger@0.15.0` and `aeturnal/super-pi-messenger` repository, homepage, and issue URLs. The lockfile root and manifest both report `0.15.0`.
- Final self-check: the provisional-state scan required by the plan returned no matches. The pre-close `git status --short --branch` showed only `## integrate/upstream-v0.15.0`, with no changed or untracked paths.
- Remote/tracking audit: `remote.upstream.url` remains `https://github.com/nicobailon/pi-messenger.git`; `remote.upstream.pushurl` is exactly `DISABLED`; `integrate/upstream-v0.15.0` has no configured tracking branch. The branch is not contained by `main` and is the only branch containing its HEAD.

### Final Review Decision

Self-review found no unaccounted production path, missing trailer, author mismatch, or metadata/remote discrepancy. Deferred minor coverage remains: requested-below-config inverse lobby cap, literal-union TypeBox schema shape, native npm package-directory refusal paths, explicit token/progress and artifact metadata preservation, and missing-final-newline feed regression. The model/frontmatter and existing warm-worker launch-time-model limitations, pre-existing untouched-eval diagnostics from the capped full-worktree scan, npm vulnerability/install-script warnings, and no-live-eval policy remain risks described above.

Ready for human review only. This branch has not been merged, pushed, published, or released; upstream push remains disabled. No tracking, push, merge, publication, release, or worktree removal action was performed by this audit.

### Post-Task 10 test-only follow-up

- Narrowed the targeted Crew test assertions over `result.details` using Vitest shape matchers and local runtime-checked subtask ID helpers. The production `Record<string, unknown>` result contract remains unchanged.

### Final diagnostic and current-HEAD verification reconciliation

- Controller-observed explicit LSP scans found 0 diagnostics in `crew/` (41 files), `tests/crew/` (40 files), and 19 changed root/test paths. The scans cover all 87 integration-changed TypeScript paths, which are clean.
- A capped full-worktree scan reports pre-existing diagnostics only in untouched eval tests. Equivalent diagnostics reproduce on fork `main`; this report therefore does not claim the whole historical repository is LSP-clean.
- Reviewed targeted test-typing commits: `d030db9` (lobby fixtures), `fa4aa63` (spawn nullable mock), `2845b6c` plus `2ca823b` (typed details assertions and feedback exactness), and `d4f341e` (AgentProgress fixtures). These adaptations are test-only; production behavior was unchanged.
- Controller-observed current-HEAD commands passed: `npm ci`; `tsc --noEmit`; full Vitest (58 files / 701 tests); deterministic evals (8 files / 197 tests); `npm pack --dry-run` (62 files); and metadata, diff, and commit checks. npm still reports 3 existing vulnerabilities and two install-script approval warnings.
- This documentation-only correction did not rerun tests; it records the controller evidence above.

### Final review targeted fix 1: unconditional Crew-child authorization marker

- Critical finding: inactive Superpowers launches gave planner, reviewer, and analyst subprocesses the `pi_messenger` extension without an authorization marker recognized by `isCrewChildProcess()`. Lobby subprocesses also lacked the role marker and were recognized only through their separate lobby/worker markers. The binding rule is now explicit: every Crew child is denied `task.approve` and `task.reject`, regardless of Superpowers state.
- Root cause: the shared `runAgent()` launch path derived the Crew role for every child, but placed `PI_CREW_ROLE` inside the conditional `superpowersFlag`. When `prepareSuperpowersLaunch()` returned no active launch, non-worker roles received no marker. The separate lobby spawn boundary never set `PI_CREW_ROLE`.
- Strict TDD RED: `npm exec vitest -- run tests/crew/superpowers-launch.test.ts tests/crew/lobby.test.ts tests/crew/router-status.test.ts` exited `1` with seven expected launch failures. Inactive planner, reviewer, analyst, and worker launches, both inactive/fallback worker compatibility assertions, and lobby launch all showed the missing role marker. The router matrix already denied approval/rejection when supplied the required planner, reviewer, analyst, worker, and lobby marker values, isolating the fault to spawn environment propagation.
- GREEN implementation commit: `74e0a22399d5bc2d8b1402002a025573fc910f34` (`fix: mark every Crew child process`). `runAgent()` now always sets its derived `PI_CREW_ROLE`; `spawnLobbyWorker()` always sets `PI_CREW_ROLE=worker`. Only the Superpowers guard extension, child flag, and generated guidance remain conditional. Model routing, prompts, and Team behavior were not changed.
- Focused GREEN: the RED command passed with `3` files / `52` tests. The broader launch/guard/routing/approval command passed with `8` files / `93` tests: Superpowers launch and guard, lobby, router status, Team routing, Team task approval, model routing, and task actions.
- Verification: `npm exec tsc -- --noEmit` exited `0` with no output; `npm test` exited `0` with `58` files / `711` tests; `npm exec vitest -- run tests/evals` exited `0` with `8` files / `197` tests; `git diff --check` and `git show --check 74e0a22` exited `0`. This API harness exposes no Pi `lsp_diagnostics` tool, so no fresh per-file LSP request was available; the TypeScript check is clean.
- Self-review: both Crew subprocess creation sites (`crew/agents.ts` and `crew/lobby.ts`) now set the role marker after configurable environment overrides, so configuration cannot remove or replace the authorization identity. Existing worker and lobby compatibility markers remain. No new Critical or Important concern was found. No push, merge, publication, release, or live model-backed eval occurred.

### Final review targeted fix 2: overlay current provider-qualified host model routing

- Important finding: overlay manual task starts, concurrency task starts, post-planning auto-spawn, auto-refill, and new lobby launches routed through persisted `MessengerState.model`. That field is registry/display state, stores only the model ID in normal store updates, and can lag a host model switch. Overlay revision actions passed no session model. The binding rule is now explicit: an overlay action reads the current provider-qualified host session model at action time; persisted display state does not select execution routing.
- Root-cause trace: normal Crew tool actions already derive `${ctx.model.provider}/${ctx.model.id}` in `crew/index.ts`. Both `MessengerOverlay` construction sites in `index.ts` discarded the current `ExtensionContext`, so `overlay.ts` could only reach stale `state.model`, while `overlay-actions.ts` invoked `executeRevise`/`executeReviseTree` without the available session-model argument. The launch resolvers below these boundaries were already correct and preserve task → request → Team role → Crew config → host session → agent frontmatter precedence.
- Strict TDD RED: `npm exec vitest -- run tests/overlay.test.ts` exited `1` with five expected failures. Manual worker, new lobby, post-planning auto-spawn, and auto-refill calls received stale `state.model`; revision received no fifth session-model argument. Each regression switches the accessor from `openai-codex/gpt-5.6-terra` to `anthropic/claude-opus-4-6` after overlay construction and checks the action-time value.
- GREEN implementation commit: `6cca7c9f5178d3982c3a96a32f30215c437feada` (`fix: route overlay launches through current host model`). `index.ts` injects a small accessor at both manual and automatic overlay construction boundaries. It qualifies the current context as `provider/id` when called and returns no persisted-state fallback. All overlay launch paths call it immediately before launch, and revision forwards that value to the existing revision handler model parameter.
- Focused GREEN: `npm exec vitest -- run tests/overlay.test.ts` passed with `1` file / `10` tests. The broader overlay/model/lobby/revision/Team/Superpowers command passed with `14` files / `155` tests. Existing model and lobby matrices prove task, request, Team role, and Crew config still override the host session value; existing warm lobby workers remain assignments to their original processes and therefore retain their launch-time model.
- Follow-up coverage: `tests/overlay.test.ts` adds one concurrency-increase ready-task regression. After construction, it switches the host accessor to `anthropic/claude-opus-4-6`, presses `+`, and verifies the ready-task launch receives that provider-qualified action-time value rather than stale state. The task's stronger `task-override` remains in the task record for the downstream resolver. Focused overlay coverage is now `1` file / `11` tests.
- Targeted follow-up coverage: the tree-revision dispatch switches the host accessor after overlay construction, presses `Shift+P`, and verifies `overlay-actions.ts` forwards `anthropic/claude-opus-4-6` to `executeReviseTree`, never the stale saved model. The existing revision handler keeps its stronger planner configuration override ahead of this fallback. Focused overlay coverage is now `1` file / `12` tests.
- Verification: `npm exec tsc -- --noEmit` exited `0` with no output; `npm test` passed with `58` files / `716` tests; deterministic evals passed with `8` files / `197` tests; `git diff --check` and `git show --check 6cca7c9` exited `0`. This API harness exposes no Pi LSP diagnostics tool, so no fresh per-file LSP request was available.
- Self-review: persisted `state.model` and store registration fields remain display/persistence data and are no longer read by overlay execution paths. No Crew-child authorization marker or provider-error code changed. The accessor is evaluated for each new launch, not cached; already-running workers are not relaunched. No new Critical or Important concern was found. No push, merge, publication, release, or live model-backed eval occurred.

### Final review targeted fix 3: common terminal provider auth and quota messages

- Important finding: the terminal assistant error boundary added in `65d9642` was correct, but its semantic matcher missed common provider messages: `401: Invalid API key`, invalid credentials, `402: Payment required`, `429: Too many requests`, and resource-exhausted forms. These terminal authentication, billing, and quota failures could therefore leave Crew retrying even though fail-fast behavior was already documented.
- Root cause: after requiring `message_update` or `message_end`, assistant role, `stopReason: "error"`, and an allowlisted formatted or structured 4xx status, `getTerminalProviderError()` applied a phrase expression containing generic markers such as `auth`, `quota`, and `rate limit`. The expression did not contain the real phrases above. Event classification, status extraction, and the process termination path were not at fault.
- Strict TDD RED: `npm exec vitest -- run tests/crew/agent-events.test.ts` exited `1` with exactly `10` expected realistic-message failures and `9` passes. Both `message_update` and `message_end` failed to terminate for invalid API key, invalid credential, payment-required, too-many-requests, and resource-exhausted messages. Ordinary assistant content, tool output, a non-error stop reason, unrelated allowlisted 4xx text, unrelated 4xx status, and the existing quota/provider cases did not produce false fail-fast matches.
- GREEN implementation commit: `2bb47a3` (`fix: recognize common terminal provider errors`), 2 files changed with 108 insertions and 1 deletion. The only production edit adds the missing phrases to the existing semantic expression. It preserves the terminal assistant plus error stop-reason plus allowlisted-4xx boundary, useful original error text, synthetic `provider_error` compatibility, and existing quota/auth behavior. Compaction, model routing, authentication markers, and arbitrary content scanning are unchanged.
- Focused GREEN: `npm exec vitest -- run tests/crew/agent-events.test.ts tests/crew/live-progress.test.ts tests/crew/utils/artifacts.test.ts` passed with `3` files / `26` tests. `npm exec tsc -- --noEmit` exited `0` with no output. `git diff --check`, `git show --check --stat --oneline 2bb47a3`, and the commit-range diff check exited `0`. This API harness exposes no Pi LSP diagnostics tool, so no fresh per-file LSP request was available.
- Full verification: `npm test` passed with `58` files / `733` tests. Deterministic evals passed with `8` files / `197` tests. No live model-backed eval ran because it could consume provider quota and no approval was given.
- Self-review: positive coverage exercises both supported terminal assistant event types for every new phrase. Negative coverage proves the matcher does not scan ordinary assistant content or tool output and still requires the error stop reason, semantic relevance, and allowlisted status. The broader word `resource` alone is not accepted; only resource-exhausted forms match. No new Critical or Important concern was found. No push, merge, publication, release, or live model-backed eval occurred.

## Post-integration independent review repairs

Independent review after the original upstream integration found boundary, lifecycle, review, and provider-error gaps. The repairs preserve the existing Crew-only dispatcher, child isolation, Team approval gates, lobby lifecycle, review sequencing, and provider-event boundary.

### Findings and repairs

- Crew children are fail-closed for Crew actions: they cannot administer or dispatch Team, register or invoke nested Crew workflows, or mutate another worker's task. The command guard is intentionally narrow: it blocks direct `pi`/`npx pi` command segments and mutating `git worktree add`, `remove`, `move`, or `prune` commands; indirect shell execution remains outside this guard. Active tasks also reject structural mutation, and tree revision preserves Team approval metadata.
- The overlay is passive; only explicit Crew work paths dispatch. Lobby workers are matched by cwd, model, role, and Superpowers mode; they share the same concurrency budget and result/retry/review lifecycle as fresh workers.
- Automatic implementations do not bypass review. Missing review prerequisites or exhausted review iterations block the task, `NEEDS_WORK` retries only within the configured limit, and only `SHIP` keeps the task done.
- Temporary generic 429 rate limits remain retryable. Fail-fast provider handling supports either a terminal assistant `message_update`/`message_end` event with `stopReason: "error"` or a `provider_error` event; both require an allowlisted status (`400`, `401`, `402`, `403`, or `429`) and durable credential, billing, or account-quota evidence scoped to that status.

### Repair commits

- Child boundary and task safety: `62bee20`, `c2f798a`, `3cb2030`, `a1e93b3`, `de73a5a`, `b08b551`, `a965f32`, `6a92169`, `0c2306b`, `e41723d`, `28b4380`, `ff86361`.
- Lobby dispatch and lifecycle: `c38e657`, `37dade5`, `4246b10`, `4e10fc3`, `a3302dc`, `a7387cf`, `cb94afc`, `e31807e`, `cd6cc13`, `8b6db5e`, `a474465`, `6b5a04b`.
- Retry and automatic review: `348ad9c`, `3de4830`, `394aa67`, `575a3f8`, `ef497f8`, `5f2a4e6`, `b43ef84`.
- Provider error classification: `4b915d7`, `9c82247`, `1153afd`, `1fa36a5`.

### Test evidence and deferred risks

Repairs received focused coverage, with RED/GREEN evidence recorded where behavior changed. Task 19's focused review/dependency suite passed on its first run and made no production change, so it has no RED phase. The final automatic-review regression suite covered 85 review/store/lifecycle tests; the final provider-event suite covered 41 classification cases; focused TypeScript checks passed. Documentation coverage is added separately for the user-facing safety statements.

Deferred risks remain: the Task 1–20 repairs did not rerun the complete repository suite or a live provider-backed evaluation. A live evaluation may consume provider credits and requires approval. Existing documented limitations remain: an already-running lobby worker retains its launch-time model, and project-local frontmatter can select a model when stronger sources are absent. This report records local integration work only; it does not claim publication or release.

## Fix Round 1 — Task 21 documentation corrections

- Qualified the README and repair summary: the runtime command guard covers direct `pi`/`npx pi` command segments and mutating `git worktree add`, `remove`, `move`, or `prune` commands. Indirect shell execution remains outside this narrow guard.
- Documented both supported fail-fast event forms: terminal assistant `message_update`/`message_end` events with `stopReason: "error"` and `provider_error` events. Both require an allowlisted status and durable credential, billing, or account-quota evidence; generic temporary 429 throttling remains retryable.
- Corrected the repair evidence claim: Task 19 passed its focused suite on the first run, so the report now says focused coverage with RED/GREEN evidence where behavior changed.
- Tightened the README branding test to assert the full qualified affiliation statement and the guarded command wording.
- Verification: `npm exec vitest -- run tests/readme-branding.test.ts` — exit `0`; 1 file / 4 tests passed. `npm exec tsc -- --noEmit` — exit `0`; no output.
- Scope: documentation and test wording only; no production behavior changed.
