# Upstream v0.15.0 Integration Report

## Source

- Upstream: `https://github.com/nicobailon/pi-messenger.git`
- Tag: `v0.15.0` at `2f5e7dc9c77fd7a3fba4728931e8564ce48d9bab`
- Shared base: `f836c400103bb3c99ff350aa9c3d0e80e58f046b`
- Fork base: `3315090`
- Integration branch: `integrate/upstream-v0.15.0`
- Upstream push URL: `DISABLED`

## Baseline Verification

Date: `2026-08-04T01:04:53-05:00`

Commands and observed results:

- `test "$(git branch --show-current)" = "integrate/upstream-v0.15.0"` — exit `0`.
- `test "$(git rev-parse 2f5e7dc)" = "2f5e7dc9c77fd7a3fba4728931e8564ce48d9bab"` — exit `0`.
- `test "$(git merge-base 3315090 upstream/main)" = "f836c400103bb3c99ff350aa9c3d0e80e58f046b"` — exit `0`.
- `test "$(git config --get remote.upstream.pushurl)" = "DISABLED"` — exit `0`.
- `git status --short --branch` — `## integrate/upstream-v0.15.0`.
- `npm ci` — exit `0`; 191 packages added and 192 packages audited. npm reported 3 vulnerabilities (1 moderate, 2 high) and install-script approval warnings for `@google/genai@1.52.0` and `protobufjs@7.6.5`.
- `npm exec tsc -- --noEmit` — exit `0`; no output.
- `npm test` — exit `0`; Vitest `45` test files passed and `607` tests passed.
- `git diff --check` — exit `0`.
- `git status --short` — no output before this report was created.

`package.json`, `package-lock.json`, and `docs/upstream-maintenance.md` were unchanged by installation and baseline checks.

## Commit Ledger

| Upstream commit | Subject | Disposition | Fork adaptations | Tests |
|---|---|---|---|---|
| `2c3b748` | Optional Crew Team layer | Adapted | Cherry-picked intact as `ad4fb91` with upstream authorship and `-x` trailer; one fork-only type import adaptation in follow-up `32d5e2d`: `@mariozechner/pi-coding-agent` → installed `@earendil-works/pi-coding-agent` in `crew/handlers/team.ts`; boundary-hardening adaptation `c3eb4fe`: controller-only approval/rejection, Crew child denial and command removal, post-role Crew-only restrictions, and bounded Team memory handling | Initial Team + worker coordination: 7 files / 61 tests; initial Superpowers boundary: 5 files / 58 tests; initial full suite: 51 files / 651 tests; boundary hardening: 13 relevant Team/Superpowers files / 134 tests; `tsc --noEmit`: pass |
| `8d24757` | Team workflows for current Pi | Adapted | Cherry-picked as `f58620e` with upstream authorship and `-x` trailer; composed current-Pi Team workflows with fork-owned Superpowers launch/policy/status, controller-only approvals, post-role Crew restrictions, bounded Team memory, fork package identity/dependency floors, and combined Team/Superpowers status. Follow-up `356aa55` hardens Crew status by separating executable, pending-approval, and rejected tasks, with distinct work, approval, and revision guidance; it also corrects changelog packaging text to match the manifest. | Team focus: 12 files / 160 tests; status hardening RED: 3 expected failures / 11 passes; GREEN: 14/14; Team/status: 163/163; Superpowers: 62/62; `tsc --noEmit` and `git diff --check`: pass; full suite: 51 files / 663 tests |
| `5cb1f53` | Backlog compatibility fixes | Not yet imported | None recorded | Not yet run |
| `817e1ae` | Legacy installer package-conflict guard | Not yet imported | None recorded | Not yet run |
| `bd76c63` | Crew session-model inheritance | Not yet imported | None recorded | Not yet run |
| `a97fbf2` | Compact Crew event artifacts | Not yet imported | None recorded | Not yet run |
| `2e83f14` | Bounded feed reads and task snapshots | Not yet imported | None recorded | Not yet run |
| `2f5e7dc` | v0.15.0 release metadata | Not yet imported | None recorded | Not yet run |

## Conflict Decisions

- `2c3b748` `.gitignore` content conflict: retained the fork's `node_modules/`, `npm-shrinkwrap.json`, `*.DS_Store`, `.pi/`, and `.worktrees/` rules; retained upstream's `# Local agent scratch` and `progress.md`; did not restore `package-lock.json`, because this fork intentionally commits its lockfile.
- `2c3b748` `CHANGELOG.md` content conflict: retained the fork's existing `Unreleased / Added` Superpowers authorization entry and added upstream's optional Team-layer entry below it. No prior history was replaced.
- No other files conflicted. Conflict-marker scan found none after resolution; `git diff --check` passed.
- `8d24757` produced content conflicts in `config-overlay.ts`, `crew/agents.ts`, `crew/handlers/plan.ts`, `crew/handlers/review.ts`, `crew/handlers/status.ts`, `crew/handlers/sync.ts`, `crew/handlers/task.ts`, `crew/handlers/team.ts`, `crew/handlers/work.ts`, `crew/index.ts`, `index.ts`, `overlay-actions.ts`, `tests/crew/status.test.ts`, `tests/crew/team-routing.test.ts`, `tests/crew/team-task-approval.test.ts`, `tests/crew/worker-coordination.test.ts`, `tests/mention-autocomplete.test.ts`, and `tsconfig.json`.
- Mechanical `8d24757` resolutions accepted current-Pi `.ts` import suffixes, explicit `ctx.cwd` flow, and combined TypeScript options (`allowImportingTsExtensions` plus the fork's Node types). Both upstream and fork test cases were retained where adjacent additions conflicted.
- Meaningful `8d24757` resolutions were dictated by approved constraints: retained Superpowers selection/provenance/child guard/native fallback in `crew/agents.ts`; retained outer Superpowers policy and loaded-skill capture in `index.ts`; retained controller-only approval guards in both router and handler; kept worker coordination free of approval/rejection commands while adding rejected-task revision guidance; kept the post-role Crew-only restriction and bounded memory behavior; and exposed Team status before fork-owned Superpowers status.
- `8d24757` package metadata auto-merged upstream peer-dependency wildcards. The resolution restored the fork manifest unchanged: `super-pi-messenger@0.14.1`, `typebox` runtime floor `^1.1.24`, current `@earendil-works` development floors, Node `>=24`, repository metadata, and package manager. No package-lock change was made.

## Checkpoint Results

### `2c3b748` Optional Crew Team Foundation

- Cherry-picked with `git cherry-pick -x 2c3b748` as `ad4fb911f37201d06232d874de30961c3923ccde`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-05-02 23:51:01 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 01:08:05 -0500`).
  - Preserved trailer: `(cherry picked from commit 2c3b748559de829207a191240c166788e4f3577f)`.
  - Import result: 37 files changed; 2,447 insertions and 110 deletions after the required conflict resolutions.
- Fork-only compatibility adaptation: the imported `crew/handlers/team.ts` referenced `@mariozechner/pi-coding-agent`, absent from this fork's manifest and `node_modules`. The existing fork uses `@earendil-works/pi-coding-agent`; follow-up `32d5e2d1fc8dd65ebfecfba0ccf9f6ec01cd1fd8` changes that type-only import. This was required because the prescribed `npm exec tsc -- --noEmit` first failed with `TS2307`; it passed after the one-line adaptation.
- Authorization/execution-boundary review: Team code discovers role metadata and routes all work through existing `crew/spawn.ts`, `crew/lobby.ts`, `crew/handlers/work.ts`, `index.ts`, and `overlay.ts` Crew paths. `crew/handlers/team.ts` does not spawn or launch a subagent; it explicitly states that Team roles are labels/prompt context and Crew remains the execution engine. No new launch path was found.
- Verification after the adaptation:
  - `npm exec vitest -- run tests/crew/team-overlay-render.test.ts tests/crew/team-routing.test.ts tests/crew/team-store.test.ts tests/crew/team-subagent-roles.test.ts tests/crew/team-task-approval.test.ts tests/crew/team-work.test.ts tests/crew/worker-coordination.test.ts` — exit `0`; 7 files and 61 tests passed.
  - `npm exec vitest -- run tests/crew/superpowers.test.ts tests/crew/superpowers-policy.test.ts tests/crew/superpowers-guard.test.ts tests/crew/superpowers-launch.test.ts tests/crew/superpowers-extension.test.ts` — exit `0`; 5 files and 58 tests passed.
  - `npm exec tsc -- --noEmit` — exit `0`; no output.
  - `npm test` — exit `0`; 51 files and 651 tests passed.

### `8d24757` Current-Pi Team Workflows

- Cherry-picked with `git cherry-pick -x 8d24757` as `f58620ed1d723b84824e41897eaf68c418b72345`.
  - Author preserved: `Nico Bailon <nico.bailon@gmail.com>` (`2026-07-30 14:33:17 -0700`).
  - Committer: `aeturnal <dominic@aeturnal.io>` (`2026-08-04 01:38:41 -0500`).
  - Preserved trailer: `(cherry picked from commit 8d24757494064770411d23997f73bff61d8498a3)`.
  - Import result: 76 files changed; 966 insertions and 608 deletions after conflict composition.
- Semantic gate decisions:
  - Team setup, profile, charter, memory, roles, approvals, revisions, work, and overlays continue through existing Crew routes; Team handlers do not introduce a second launcher.
  - `crew/agents.ts` retains fork-owned Superpowers selection, loaded-skill provenance, child guard extension/environment, role metadata, ordered guidance, and native fallback.
  - Team role/charter/memory context stays in the Crew task prompt. The higher-priority Crew-only restriction remains after imported Team text. Superpowers system guidance remains fork-owned and is not duplicated by Team context.
  - Pending approval and rejected revision states remain in `.pi/messenger/crew/` task records. Crew child markers cannot approve or reject. Rejected tasks are reported separately and return to work only through revision actions.
  - Overlay actions use existing Crew task and spawn controls; no new unreviewed dispatch path was added.
  - Fork package identity and dependency floors were retained unchanged.
- RED/GREEN adaptation evidence:
  - Added `tests/crew/status.test.ts` coverage requiring active Team details and fork-owned Superpowers details in one Crew status response, with Team displayed before Superpowers.
  - RED: `npm exec vitest -- run tests/crew/status.test.ts` exited 1 with 1 expected failure because `## Team` and `details.team` were absent.
  - GREEN: the same command exited 0 with 1 file / 11 tests after the smallest status composition.
  - Conflict-composition focus then passed: 8 files / 83 tests.
- Checkpoint A verification:
  - Team focus command: exit 0; 12 files / 160 tests.
  - Superpowers boundary command: exit 0; 7 files / 62 tests.
  - `npm exec tsc -- --noEmit`: exit 0; no output.
  - `npm test`: exit 0; 51 files / 663 tests.
  - The requested `lsp_diagnostics` pass could not be run because this harness exposes no LSP diagnostics tool; TypeScript checking covered all transitively imported TypeScript files.
  - Conflict-marker scan, `git diff --check`, and package identity checks passed before cherry-pick continuation.

### Boundary-hardening adaptation (`c3eb4fe`)

- User decision: task approval and rejection are controller-only. Every Crew child process is denied, and worker coordination text no longer provides approval or rejection commands.
- User decision: a higher-priority Crew-only restriction is appended after imported Team role text, so imported roles cannot override Crew execution boundaries.
- Correctness adaptation: Team profile `memory.maxCharsPerType` must be a positive, bounded integer (1 through 10,000); Team memory entries that exceed the per-type prompt budget are skipped, including an oversized first entry.
- Adaptation commit: `c3eb4feda32629f881ca2b9e948ce5db8cd4d7c8 fix: harden Crew Team approval boundaries`.
- TDD evidence: the new boundary tests first failed (RED) for all three review findings, then passed after the adaptation (GREEN). The focused boundary result was `13` relevant Team/Superpowers test files and `134` tests passed; `npm exec tsc -- --noEmit` passed.
- Task review result: all three findings were addressed. Review found no new Critical or Important breakage.

### Crew status hardening adaptation (`356aa55`)

- Adaptation commit `356aa55` separates executable, pending-approval, and rejected tasks in Crew status.
- Only executable tasks receive ready/work guidance; pending receives approval guidance; rejected receives revision guidance.
- Changelog packaging text was corrected to match typebox runtime and Pi development dependencies.
- TDD evidence: RED status suite 3 expected failures / 11 passes; GREEN 14/14; Team/status 163/163; Superpowers 62/62; TypeScript and diff check passed.
- Scoped re-review found both issues addressed and no new Critical/Important breakage.

## Remaining Risks

Model precedence, installer identity, and fork release version require explicit review during later checkpoints. The harness lacked `lsp_diagnostics`; compiler, focused, and full-suite verification passed, but editor-specific diagnostics were not available.

## Final Review Decision

The branch is not ready for merge, push, publication, or release.
